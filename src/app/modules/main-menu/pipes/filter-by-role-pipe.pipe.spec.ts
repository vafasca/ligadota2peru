import { FilterByRolePipePipe } from './filter-by-role-pipe.pipe';

describe('FilterByRolePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByRolePipePipe();
    expect(pipe).toBeTruthy();
  });
});
